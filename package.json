{
  "name": "kira-app",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently -k \"source .venv/bin/activate && python3 backend/initialize_models.py && python3 backend/main.py\" \"cd frontend && pnpm start\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}